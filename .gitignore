node_modules
dist
demo-dist
out-tsc
coverage
test-output
*.tsbuildinfo
.vite
.DS_Store

# Yarn 4 (Berry) — node-modules linker, not zero-install.
# Ignore caches/state; keep pinned releases/plugins/patches/sdks if added later.
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*
